import {StyleSheet} from 'react-native';

export const loginElementStyles = StyleSheet.create({
  label: {
    fontSize: 15,
  },
  input: {
    borderRadius: 3,
    borderWidth: 2,
    padding: 10,
    marginBottom: 15,
  },
  errorText: {
    fontSize: 15,
    color: '#c0392b',
  },
});
